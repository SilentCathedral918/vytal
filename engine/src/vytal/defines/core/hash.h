#pragma once

#include "types.h"

// hashed integer value ------------------------------------------------- //

typedef UInt64 HashedInt;

// hashed modes --------------------------------------------------------- //

typedef enum Hash_Mode { HASH_MODE_XX64 } HashMode;
